@{
  Layout = "_Layout";
}

@using SweetAndSavory.Models;
@model IEnumerable<SweetAndSavory.Models.Flavor>

<div class="container">
  <h2 class="text-center">Flavors for @User.Identity.Name</h2>

  @if (@Model.Any())
  {
    <ul>
      @foreach (Flavor flavor in Model)
      {
        <li>@Html.ActionLink($"{flavor.FlavorName}", "Details", new { id = flavor.FlavorId }) </li>
      }
    </ul>
  } 
  else 
  {
    <h3> No flavors have been added yet </h3>
  }

  <p class="text-center navMenu" >@Html.ActionLink("Add new flavor", "Create")</p>
</div>

